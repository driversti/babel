[project]
name = "babel"
version = "0.1.0"
description = "Archive of every eRepublik article and its comments"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.30.0",
    "curl-cffi>=0.13.0",
    "aiohttp>=3.13.0",
    "selectolax>=0.3.21",
    "pydantic>=2.12.0",
    "pydantic-settings>=2.11.0",
    "click>=8.3.0",
]

[project.scripts]
babel = "babel.cli:main"

[dependency-groups]
dev = [
    "pytest>=9.0.0",
    "pytest-asyncio>=1.3.0",
    "testcontainers[postgres]>=4.14.0",
    "ruff>=0.15.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/babel"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "B", "UP", "ASYNC", "RET", "SIM"]
ignore = ["E501"]
